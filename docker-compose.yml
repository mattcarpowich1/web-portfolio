version: '3.2'
services: 
  backend:
    build: ./app
    networks: 
      - docker-network

  loadbalancer:
    build: ./load-balancer
    ports:
      - '80:80'
    networks:
      - docker-network

volumes:
  backend:

networks:
  docker-network:
    driver: bridge